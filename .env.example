# Example environment variables for Changelog App

# GitLab API token with read_api scope for private repositories
# This token is used to authenticate with GitLab API for fetching repository data
# You can create a personal access token at https://gitlab.com/-/profile/personal_access_tokens
GITLAB_API_TOKEN=your_gitlab_api_token_here

# Application configuration
NEXT_PUBLIC_APP_URL=http://localhost:3000